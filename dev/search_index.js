var documenterSearchIndex = {"docs":
[{"category":"section","location":"#BatchSolve","page":"Home","text":"Documentation for BatchSolve.\n\n","title":"BatchSolve"},{"category":"method","location":"#BatchSolve._brent!-Union{Tuple{T}, Tuple{Function, Any, AbstractArray{T}}} where T","page":"Home","text":"_brent!(g!, y, x; xa, xb, tol, maxiter, check_every, iters, retcode) -> NamedTuple\n\nCore fully-vectorized Brent minimization loop. Finds the minimum of g! over [xa[i], xb[i]] for every element i simultaneously, with no scalar indexing. Implemented as a @generated function to ensure compile-time constant folding of CGOLD and eps(T) for Float32 GPU compatibility.\n\nAll operations are broadcast (@.) so the implementation is device-agnostic (CPU, CUDA, Metal, etc.). There is no batchdim argument because the method is inherently one-dimensional: every element is its own independent 1-D search.\n\nArguments\n\ng!: Two-argument in-place callable g!(y, x) - contexts already captured.\ny: Objective-value array (mutated in-place). Must satisfy length(xa) == length(xb) == length(x) == length(y).\nx: Iterate array (mutated in-place; holds the minimizer on return).\n\nKeyword Arguments\n\nxa: Lower bounds array.\nxb: Upper bounds array.\ntol: Convergence tolerance used in the stopping criterion |x - mid| ≤ 2·tol·|x| + ε; default T(1e-13).\nmaxiter: Maximum number of Brent iterations per element; default 500.\ncheck_every: Number of iterations between mid-loop convergence checks. Set to 0 to skip all mid-loop checks (one final check always runs on return). Higher values reduce host–device synchronisation overhead on GPU when g! is expensive; default 1.\niters: Pre-allocated Int array (same shape as x) in which the iteration index at convergence is recorded per element. Allocated automatically by default.\nretcode: Pre-allocated UInt8 array (same shape as x) for per-element return codes. Allocated automatically by default.\n\nAlgorithm\n\nImplements the classical Brent (1973) minimization algorithm, extended to operate on entire arrays without branching:\n\nInitialisation — the golden-section point xa + CGOLD·(xb - xa) is used as the starting iterate, where CGOLD = (3 - √5) / 2 ≈ 0.382.\nParabolic interpolation — on each iteration a parabola is fit through the three best points (x, w, v). The parabolic step is accepted when it falls inside the bracket and is smaller than half the step from two iterations ago (the standard Brent acceptance criteria).\nGolden-section fallback — when the parabolic step is rejected the algorithm takes a golden-section step into the larger sub-interval.\nEdge guard — if the accepted trial point would land within 2·tol1 of a bracket endpoint, it is shifted to exactly ±tol1 from the current best point.\nBracket & point update — (xa, xb) and the three auxiliary points (x, w, v) are updated in fully-masked broadcast expressions so that converged lanes are never modified.\n\nScratch arrays\n\nTen T-valued and five Bool-valued temporary arrays are allocated once before the loop. They are reused across iterations (and across logical roles within a single iteration) to minimise allocation. See inline comments in the source for the reuse map.\n\nReturns\n\nA NamedTuple with fields:\n\nu: Minimizer array (x, mutated in-place).\nf: Objective value at the minimizer (y, mutated in-place).\niters: Per-element iteration count at convergence (Int array; -1 for elements that did not converge).\nretcode: Per-element return code (UInt8 array): RETCODE_SUCCESS if converged, RETCODE_MAXITER if not.\n\n\n\n\n\n","title":"BatchSolve._brent!"},{"category":"method","location":"#BatchSolve.brent!-Tuple{Function, AbstractArray, AbstractArray, Vararg{Any}}","page":"Home","text":"brent!(g!, y, x, contexts...; xa, xb, tol, maxiter, check_every) -> NamedTuple\n\nIn-place Brent minimizer. Same as brent but mutates y and x directly, avoiding allocation. x holds the minimizer on return.\n\nArguments\n\ng!: In-place objective g!(y, x, contexts...), mutating y.\ny: Objective-value array (mutated).\nx: Initial guess (mutated; holds the solution on return).\ncontexts: Optional context objects or plain values forwarded to g!.\n\nKeyword Arguments\n\nxa: Array of lower bounds (same length as x).\nxb: Array of upper bounds (same length as x).\ntol: Convergence tolerance; default eltype(x)(1e-13).\nmaxiter: Maximum iterations; default 500.\ncheck_every: Iterations between convergence checks; default 1. Higher values reduce host–device sync overhead on GPU. Set to 0 to skip mid-loop checks (a final check always runs on return).\n\nReturns\n\nA NamedTuple with fields:\n\nu: Minimizer (x, mutated in-place).\nf: Objective value at the minimizer (y, mutated in-place).\niters: Per-element iteration count at convergence (-1 if not converged).\nretcode: RETCODE_SUCCESS or RETCODE_MAXITER per element.\n\n\n\n\n\n","title":"BatchSolve.brent!"},{"category":"method","location":"#BatchSolve.brent-Tuple{Any, AbstractArray, Vararg{Any}}","page":"Home","text":"brent(g, x, contexts...; xa, xb, tol, maxiter, check_every) -> NamedTuple\n\nFinds the minimum of the vectorized function g over [xa[i], xb[i]] for each x[i] using Brent's method.\n\nArguments\n\ng: Objective function called as g(x, unwrapped_contexts...). Must accept and return arrays on the same device as x.\nx: Initial guess array (x cannot be a Number!); device is inferred from this.\ncontexts: Optional Constant or Cache objects, or plain values, forwarded to g.\n\nKeyword Arguments\n\nxa: Array of lower bounds (same length as x).\nxb: Array of upper bounds (same length as x).\ntol: Convergence tolerance; default eltype(x)(1e-13).\nmaxiter: Maximum iterations; default 500.\ncheck_every: Iterations between convergence checks; default 1. Higher values reduce host–device sync overhead on GPU. Set to 0 to skip mid-loop checks (a final check always runs on return).\n\nReturns\n\nA NamedTuple with fields:\n\nu: Minimizer (x, mutated in-place).\nf: Objective value at the minimizer (y, mutated in-place).\niters: Per-element iteration count at convergence (-1 if not converged).\nretcode: RETCODE_SUCCESS or RETCODE_MAXITER per element.\n\n\n\n\n\n","title":"BatchSolve.brent"},{"category":"method","location":"#BatchSolve.levenberg_marquardt!-Union{Tuple{T}, Tuple{Function, Function, Any, Any, Any, Vararg{DifferentiationInterface.Context}}} where T","page":"Home","text":"levenberg_marquardt!(val!, val_and_jac!, y, jac, x, contexts...; reltol, abstol, gradtol,\n                     maxiter, batchdim, iters, retcode, solver, verbose, dx, damping,\n                     damping_min, damping_max, accept_ratio, diagmin, diagmax)\n\nCore Levenberg–Marquardt loop. Accepts a residual-only callable and a combined value-and-Jacobian callable, and iterates\n\n(JᵀJ + λ D) dx = -Jᵀy,    D = clamp(diag(JᵀJ), diagmin, diagmax)\n\nwith a Nielsen-style adaptive λ, until convergence or maxiter.\n\nArguments\n\nval!: Callable (y, x, contexts...) -> nothing filling the residual y in-place. Used for trial points, so that the (expensive) Jacobian is only evaluated once per accepted step.\nval_and_jac!: Callable (y, jac, x, contexts...) -> nothing filling y and jac.\ny: Residual array (mutated).\njac: Jacobian (mutated). Dense when batchdim=nothing; when batchdim is set, a sparse block-diagonal matrix with the layout produced by AutoBatch/make_pattern.\nx: Current iterate (mutated; holds the solution on return).\ncontexts: Optional DifferentiationInterface.Context objects.\n\nKeyword Arguments\n\nSee levenberg_marquardt. Additionally:\n\niters, retcode: Optional pre-allocated arrays of shape (B, 1) (batchdim=1) or (1, B) (batchdim=2) receiving per-problem results (Int and UInt8 respectively).\ndx: Pre-allocated step buffer; default zero.(x).\n\nAlgorithm (per batch element, all in masked array form)\n\nCompute g = Jᵀy, cost = ‖y‖². Elements whose residual (abstol) or scaled gradient (gradtol) is small are marked converged; elements with non-finite cost/g are marked failed. Marked elements are frozen: they never update x, λ, or iters again.\nSolve the damped normal equations for dx.\nEvaluate the residual at the trial point x + dx and compute the gain ratio ρ = (‖y‖² - ‖y_trial‖²) / (dxᵀ(λ D dx - g)).\nAccept the step where ρ > accept_ratio and the trial cost is finite; x is updated with ifelse, so rejected/non-finite trial points never contaminate x.\nIf any element accepted a step, re-evaluate residual and Jacobian (at unchanged x for the others, which is harmless). Accepted steps whose new residual or Jacobian is non-finite are rolled back to the previous x and counted as rejected (one extra evaluation, only when this happens). A non-finite residual/Jacobian at the initial point is a failure.\nUpdate λ ← λ max(1/3, 1 - (2ρ-1)³), ν ← 2 on acceptance, else λ ← λν, ν ← 2ν.\nElements whose (finite, evaluated) step is small relative to x (reltol) are marked converged; elements rejected while λ == damping_max are marked failed.\n\nFor batchdim=nothing the same code runs with a single \"lane\", and JᵀJ is formed with mul!. Sparse Jacobians are only supported together with batchdim.\n\nReturns\n\nSame NamedTuple as levenberg_marquardt.\n\n\n\n\n\n","title":"BatchSolve.levenberg_marquardt!"},{"category":"method","location":"#BatchSolve.levenberg_marquardt!-Union{Tuple{T}, Tuple{X}, Tuple{Y}, Tuple{Function, Y, X, Vararg{DifferentiationInterface.Context}}} where {Y, X, T}","page":"Home","text":"levenberg_marquardt!(f!, y, x, contexts...; reltol, abstol, gradtol, maxiter, autodiff, prep,\n                     batchdim, solver, verbose, dx, damping, damping_min, damping_max,\n                     accept_ratio, diagmin, diagmax)\n\nIn-place Levenberg–Marquardt solver for ‖f!(y, x, contexts...)‖². Prepares the AD Jacobian backend, allocates the Jacobian, then delegates to the core method levenberg_marquardt!(val!, val_and_jac!, y, jac, x, ...).\n\nArguments\n\nf!: In-place residual function; must satisfy f!(y, x, contexts...) and mutate y.\ny: Residual array (mutated in-place). May have a different length than x.\nx: Initial guess (mutated in-place; contains the solution on return).\ncontexts: Optional DifferentiationInterface.Context objects forwarded to f!.\n\nKeyword Arguments\n\nSee levenberg_marquardt. Additionally:\n\ndx: Pre-allocated step buffer; default zero.(x).\n\nReturns\n\nSame NamedTuple as levenberg_marquardt.\n\n\n\n\n\n","title":"BatchSolve.levenberg_marquardt!"},{"category":"method","location":"#BatchSolve.levenberg_marquardt-Tuple{Function, AbstractArray, Vararg{DifferentiationInterface.Context}}","page":"Home","text":"levenberg_marquardt(f, x, contexts...; reltol, abstol, gradtol, maxiter, autodiff, prep,\n                    batchdim, solver, verbose, damping, damping_min, damping_max,\n                    accept_ratio, diagmin, diagmax)\n\nMinimizes ‖f(x, contexts...)‖² (finds roots of f(x, contexts...) = 0 when a root exists) using the Levenberg–Marquardt method. Unlike newton, the residual may have a different length than x (over- or under-determined systems), and the iteration is globalized by an adaptive damping parameter, so it is far more tolerant of poor initial guesses and singular Jacobians.\n\nWhen batchdim is set, every batch element is an independent problem with its own damping parameter, acceptance decision and convergence status. Everything is expressed as array operations (no per-element branching), so it runs on any device supported by the linear solver.\n\nArguments\n\nf: Function returning the residual array; called as f(x, unwrapped_contexts...).\nx: Initial guess as AbstractArray.\ncontexts: Optional DifferentiationInterface.Context objects (Constants or Caches) forwarded to f after unwrapping.\n\nKeyword Arguments\n\nreltol: Step tolerance. Converged when ‖dx‖ ≤ reltol*(‖x‖ + reltol); default sqrt(eps(eltype(x))).\nabstol: Residual tolerance. Converged when ‖f(x)‖ < abstol; default sqrt(eps(eltype(y))) (inferred after the first evaluation of f).\ngradtol: Stationarity tolerance. Converged when, for every variable k, |(Jᵀf)_k| ≤ gradtol * ‖J[:,k]‖ * ‖f‖ (scale-invariant, as in MINPACK's gtol); default sqrt(eps(eltype(y))). This is what terminates least-squares problems whose minimum has a non-zero residual.\nmaxiter: Maximum number of iterations; default 100.\nautodiff: AD backend used to compute Jacobians. Defaults as in newton. Wraps in AutoBatch automatically when batchdim is set.\nprep: Pre-allocated DifferentiationInterface Jacobian preparation object.\nbatchdim: Batch dimension index (1, 2, or nothing); default nothing.\nsolver: Callable (x, A, b) -> x that solves the square (batched) system Ax=b in-place, where x and b have the same shape as the unknowns. Note that this differs from newton: here A is the damped normal-equations matrix JᵀJ + λ diag(D) (n × n per batch element, stored with the sparsity pattern make_pattern(x, x, batchdim)), not the Jacobian. Defaults to make_linear_solver(device, x, x, batchdim).\nverbose: Print iteration table (iteration, ‖y‖, ‖dx‖, max λ) when true; default false.\ndamping: Initial value of the (relative) damping parameter λ; default 1e-3.\ndamping_min, damping_max: Bounds on λ; defaults 1e-10, 1e10.\naccept_ratio: A step is accepted when (actual reduction)/(predicted reduction) exceeds this value; default 1e-4.\ndiagmin, diagmax: diag(JᵀJ) is clamped to [diagmin, diagmax] to form the Marquardt damping scale D; defaults 1e-6, 1e16.\n\nReturns\n\nA NamedTuple with fields:\n\nu: Solution array (same object as x, mutated in-place).\nf: Final residual (same object as y, mutated in-place).\njac: Final Jacobian.\nretcode: RETCODE_SUCCESS, RETCODE_FAILURE (non-finite residual/Jacobian at the current point, or steps still being rejected with the damping already at damping_max), or RETCODE_MAXITER, per batch element (scalar when batchdim=nothing). RETCODE_SUCCESS means converged to a root or a stationary point of ‖f‖²; inspect f if you need to distinguish them.\niters: Number of iterations taken (scalar, or array when batchdim is set; -1 for batch elements that did not converge).\n\n\n\n\n\n","title":"BatchSolve.levenberg_marquardt"},{"category":"method","location":"#BatchSolve.make_linear_solver-NTuple{4, Any}","page":"Home","text":"make_linear_solver(device, b, x, batchdim) -> Function\n\nConstruct and return a linear-system solver callable compatible with the given device, array shapes, and batch configuration. The returned function has the signature\n\n(x, A, b) -> x\n\nand solves A * x = b in-place, writing the solution into x.\n\nArguments\n\ndevice: KernelAbstractions backend (e.g. CPU(), CUDABackend()). \nb: Prototype output array (used for size introspection; not mutated).\nx: Prototype input array (used for size introspection; not mutated).\nbatchdim: Batch dimension (nothing, 1, or 2).\n\nReturned solver behaviour\n\nbatchdim Matrix type Behaviour\nnothing dense matrix Single A \\ b solve; writes NaN when A is singular.\n2 SparseMatrixCSC (block-diagonal, blocks contiguous in nzval) Iterates over batch index i, extracts each (n_rows × n_cols) block from nzval, solves independently.\n1 SparseMatrixCSC (interleaved columns) Iterates over batch index i, gathers every batchsize-th column, solves independently.\n\nSingular sub-matrices (detected via ArrayInterface.issingular) result in NaN being written to the corresponding slice of x so that upstream code can detect and handle failures gracefully.\n\nErrors\n\nThrows an ArgumentError-style error if batchdim ∉ {nothing, 1, 2}.\n\n\n\n\n\n","title":"BatchSolve.make_linear_solver"},{"category":"method","location":"#BatchSolve.newton!-Union{Tuple{T}, Tuple{Function, Any, Any, Any, Vararg{DifferentiationInterface.Context}}} where T","page":"Home","text":"newton!(val_and_jac!, y, jac, x, contexts...; reltol, abstol, maxiter, batchdim, iters, retcode, solver, verbose, dx)\n\nCore Newton iteration loop. Accepts a combined value-and-Jacobian callable and runs the Newton update x = x − inv(J)*f(x) until convergence or maxiter is reached. Supports both scalar (unbatched) and batched operation.\n\nArguments\n\nval_and_jac!: Callable (y, jac, x, contexts...) -> nothing that simultaneously fills the residual y and the Jacobian jac in-place.\ny: Residual vector / matrix (mutated).\njac: Jacobian array (mutated); may be dense or sparse depending on the AD backend.\nx: Current iterate (mutated; holds the solution on return).\ncontexts: Optional DifferentiationInterface.Context objects.\n\nKeyword Arguments\n\nreltol: Relative convergence tolerance on ‖dx‖ / ‖x‖; default √eps(eltype(x)).\nabstol: Absolute convergence tolerance on ‖y‖; default √eps(eltype(y)).\nmaxiter: Maximum number of iterations; default 100.\nbatchdim: Batch dimension (1, 2, or nothing); default nothing.\niters: Integer array (shape broadcastable over the batch dimension) in which the iteration count at convergence is recorded for each problem in the batch. Allocated automatically when batchdim is set; ignored (with a warning) when batchdim=nothing.\nretcode: UInt8 array (same shape as iters) recording the per-problem return code. Allocated automatically when batchdim is set.\nsolver: Linear solver (dx, jac, y) -> nothing; defaults to make_linear_solver(device, y, x, batchdim).\nverbose: Print per-iteration (iter, ‖y‖, ‖dx‖) table; default false.\ndx: Pre-allocated Newton step buffer; default zero.(x).\n\nConvergence criteria (non-batched)\n\nAn iteration is considered converged when either:\n\nnorm(y) < abstol (residual is small enough), or\nnorm(dx) < reltol * norm(x) (step is small relative to the current iterate).\n\nIn the batched case the same criteria are applied element-wise along batchdim, and only the unconverged sub-problems continue to be updated.\n\nReturns\n\nA NamedTuple with fields:\n\nu: Solution (x, mutated in-place).\nf: Final residual (y, mutated in-place).\njac: Final Jacobian.\nretcode: RETCODE_SUCCESS / RETCODE_FAILURE / RETCODE_MAXITER — scalar for non-batched runs, array for batched runs.\niters: Iteration count at convergence — scalar or array.\n\n\n\n\n\n","title":"BatchSolve.newton!"},{"category":"method","location":"#BatchSolve.newton!-Union{Tuple{T}, Tuple{X}, Tuple{Y}, Tuple{Function, Y, X, Vararg{DifferentiationInterface.Context}}} where {Y, X, T}","page":"Home","text":"newton!(f!, y, x, contexts...; reltol, abstol, maxiter, autodiff, prep, batchdim, solver, verbose, dx)\n\nIn-place Newton root-finder for f!(y, x, contexts...) = 0. Prepares the AD Jacobian backend, allocates the Jacobian matrix, then delegates to the three-argument newton!(val_and_jac!, y, jac, x, ...) core.\n\nArguments\n\nf!: In-place residual function; must satisfy f!(y, x, contexts...) and mutate y.\ny: Residual vector (mutated in-place).\nx: Initial guess (mutated in-place; contains the solution on return).\ncontexts: Optional DifferentiationInterface.Context objects forwarded to f!.\n\nKeyword Arguments\n\nreltol: Relative convergence tolerance; default sqrt(eps(eltype(x))).\nabstol: Absolute convergence tolerance; default sqrt(eps(eltype(y))).\nmaxiter: Maximum iterations; default 100.\nautodiff: AD backend for Jacobian computation. GPU arrays default to AutoForwardFromPrimitive(AutoForwardDiff()); CPU arrays default to AutoForwardDiff(). Wraps in AutoBatch automatically when batchdim is set.\nprep: Pre-allocated Jacobian preparation object; created automatically when nothing.\nbatchdim: Batch dimension (1, 2, or nothing); default nothing.\nsolver: Linear solver callable; defaults to make_linear_solver(device, y, x, batchdim).\nverbose: Print iteration table (iteration, ‖y‖, ‖dx‖) when true; default false.\ndx: Pre-allocated Newton step buffer; default zero.(x).\n\nReturns\n\nA NamedTuple with fields:\n\nu: Solution array (same object as x, mutated in-place).\nf: Final residual vector (same object as y, mutated in-place).\njac: Final Jacobian.\nretcode: RETCODE_SUCCESS, RETCODE_FAILURE, or RETCODE_MAXITER.\niters: Number of iterations taken (scalar, or array when batchdim is set).\n\n\n\n\n\n","title":"BatchSolve.newton!"},{"category":"method","location":"#BatchSolve.newton-Tuple{Function, AbstractArray, Vararg{DifferentiationInterface.Context}}","page":"Home","text":"newton(f, x, contexts...; reltol, abstol, maxiter, autodiff, prep, batchdim, solver, verbose)\n\nFinds roots of f(x, contexts...) = 0 using Newton's method. \n\nArguments\n\nf: Function returning the residual vector; called as f(x, unwrapped_contexts...)\nx: Initial guess as AbstractArray\ncontexts: Optional DifferentiationInterface.Context objects (Constants or Caches) forwarded to f after unwrapping.\n\nKeyword Arguments\n\nreltol: Relative convergence tolerance on the Newton step; default sqrt(eps(eltype(x))).\nabstol: Absolute convergence tolerance on the residual norm; default sqrt(eps(eltype(y))) (inferred after the first evaluation of f).\nmaxiter: Maximum number of Newton iterations; default 100.\nautodiff: AD backend used to compute Jacobians. Defaults to AutoForwardDiff() on CPU and AutoForwardFromPrimitive(AutoForwardDiff()) for GPU arrays. Wraps in AutoBatch  automatically when batchdim is set.\nprep: Pre-allocated DifferentiationInterface Jacobian preparation object. When nothing (default) it is created automatically on the first call.\nbatchdim: Batch dimension index (1, 2, or nothing). When set, independent Newton systems are solved in parallel along that dimension; default nothing.\nsolver: Callable (x, A, b) -> x that solves the (batched) linear system Ax=b in-place. Defaults to make_linear_solver(device, y, x, batchdim).\nverbose: Print iteration table (iteration, ‖y‖, ‖dx‖) when true; default false.\n\nReturns\n\nA NamedTuple with fields:\n\nu: Solution array (same object as x, mutated in-place).\nf: Final residual vector (same object as y, mutated in-place).\njac: Final Jacobian.\nretcode: RETCODE_SUCCESS, RETCODE_FAILURE, or RETCODE_MAXITER.\niters: Number of iterations taken (scalar, or array when batchdim is set).\n\n\n\n\n\n","title":"BatchSolve.newton"}]
}
